package report;

/**
 * Created by AADM107 on 18/04/2016.
 */
public class ExtentManager {
    /*private static ExtentReports extent;
    static ExtentHtmlReporter htmlReporter;
    public synchronized static ExtentHtmlReporter getReporter(String filePath) {
        if (htmlReporter == null) {
        	htmlReporter = new ExtentHtmlReporter(filePath);
        	htmlReporter.loadXMLConfig(new File("/sysroot/home/suman/automation/cucmberautomation/extent-config.xml"));
        }
        return htmlReporter;
    }

    public synchronized static ExtentHtmlReporter getReporter() {
        return htmlReporter;
    }

    public synchronized static void closeReporter() {
        if(htmlReporter!=null){
        	htmlReporter.flush();
            //extent.close();
        }
    }*/
}
